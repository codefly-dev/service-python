{
  description = "{{ .Service.Name.DNSCase }} development environment";

  inputs = {
    nixpkgs.url = "github:NixOS/nixpkgs/nixos-unstable";
  };

  outputs = { self, nixpkgs }:
    let
      systems = [ "x86_64-linux" "aarch64-linux" "x86_64-darwin" "aarch64-darwin" ];
      forAllSystems = f: nixpkgs.lib.genAttrs systems (system: f system);
    in
    {
      devShells = forAllSystems (system:
        let
          pkgs = nixpkgs.legacyPackages.${system};
        in
        {
          default = pkgs.mkShell {
            packages = [
              # Generic Python service — uv owns dep management; python is
              # present so the venv works even on hosts without a system
              # python. Specializations (python-fastapi, …) layer their
              # own flake with framework-specific tools.
              pkgs.uv
              pkgs.python312
              pkgs.ruff
              pkgs.pytest
            ];
          };
        });
    };
}
